import React from "react";

const Topbar = () => {
  return (
    <section
      className="bg-white"
      style={{
        height: "4.75vh",
        width: "100%",
      }}
    >
      Top-bar
    </section>
  );
};

export default Topbar;
